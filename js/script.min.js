import Paddle from"./paddle.js";import InputHandler from"./input.js";import Ball from"./ball.js";let canvas=document.querySelector("#GameScreen"),ctx=canvas.getContext("2d");const GameWidth=800,GameHeight=600;let paddle=new Paddle(800,600),ball=new Ball;new InputHandler(paddle),paddle.draw(ctx);let lastTime=0;function GameLoop(e){let a=e-lastTime;lastTime=e,ctx.clearRect(0,0,800,600),paddle.update(a),paddle.draw(ctx),ball.draw(ctx),requestAnimationFrame(GameLoop)}requestAnimationFrame(GameLoop);